export enum FileCategory {
    application = "application",
    image = "image",
    video = "video",
    audio = "audio",
    all = "all"
}

export enum ModelMessageRole {
    user = "user",
    assistant = "assistant",
    system = "system"
}